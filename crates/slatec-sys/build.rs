fn main() {
    // Native implementation selection belongs to `slatec-src`. Keeping this
    // raw-declaration build script inert guarantees that `external-backend`
    // cannot emit hidden native link instructions through `slatec-sys`.
}
