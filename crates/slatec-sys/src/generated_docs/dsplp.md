# Purpose

These are the short usage instructions; for details about other features, options and methods for defining the matrix A, see the extended usage instructions which are contained in the Long Description section below.

# Description

This canonical unsafe binding exposes original SLATEC routine `DSPLP`. Its documented behavior is taken from the selected, source-hash-verified provider prologue; the exact implementation source is [DSPLP](https://www.netlib.org/slatec/src/dsplp.f).

# Arguments

## 1. `DUSRMT`

callback `callback` argument; Fortran declaration `REAL`, Rust ABI type `reviewed unsafe extern callback function pointer`, and scalar. ) can be used to redefine entries of the matrix. The matrix entries are redefined or overwritten.  No accum- ulation is performed. Any other nonzero entry of A, defined in a previous call to DSPLP( ), remain intact. supplied sub- program, 'NAME'( ). They would indicate a mistake in the contents The callback must remain valid for the complete native call, satisfy the exact reviewed ABI, and must not unwind into Fortran. not stated by selected source not applicable or not stated by selected source not a workspace argument

## 2. `MRELAS`

output `scalar` argument; Fortran declaration `INTEGER`, Rust ABI type `*mut crate::FortranInteger`, and scalar. and NVARS, the array COSTS(*), data for the matrix A, and the bound information for the unknowns x and w, BL(*), are .LT. 0. The meaning of these values can be found by reading the diagnostic message in the output file, or by looking for NVARS). Long Description: SUBROUTINE DSPLP(DUSRMT,MRELAS,NVARS,COSTS,PRGOPT,DATTRV, number of entries. The input for the problem includes the problem dimensions, and NVARS, the array COSTS(*), data for the matrix A, and the bound information for the unknowns x and w, BL(*), is used to define (or update) row I and column J of A.) RETURN END IF END Remarks:  The values of I and J are the row and column indices for the nonzero entries of the matrix A. The value of this entry is AIJ. Set INDCAT=0 if this value defines that entry. Set INDCAT=1 if this entry is to be updated, new entry=old entry+AIJ. A value of I not between 1 and MRELAS, a value of J not between 1 and NVARS, or a value of INDCAT not equal to 0 or 1 are each errors. The contents of IFLAG(K), K=2,...,10, can be used to remember the status (of the process of defining the matrix entries) between calls to 'NAME'( ) by DSPLP( ). On entry to 'NAME'( ), only the values 1 or 2 will be in IFLAG(1).  More than 2*NVARS*MRELAS definitions of the matrix elements is considered an error because it suggests an infinite loop in the user-written subprogram 'NAME'( ).  Any matrix element not provided by 'NAME'( ) is defined to be zero. are .LT. 0. The meaning of these values can be found by reading the diagnostic message in the output file, or by looking for NVARS). Computing with the Matrix A after Calling DSPLP( ) 3 16). If SWITCH=0  option is off; DSPLP( ) chooses the initial basis. =1  option is on; user provides the initial basis. data set =MRELAS indices of basis; order is arbitrary. number of constraint equations. ITERATIONS. written by garbage.  Messages: (I1). IN ABOVE MESSAGE, I1=         0 and NVARS, the array COSTS(*), data for the matrix A, and the bound information for the unknowns x and w, BL(*), are .LT. 0. The meaning of these values can be found by reading the diagnostic message in the output file, or by looking for NVARS). Long Description: SUBROUTINE DSPLP(DUSRMT,MRELAS,NVARS,COSTS,PRGOPT,DATTRV, number of entries. The input for the problem includes the problem dimensions, and NVARS, the array COSTS(*), data for the matrix A, and the bound information for the unknowns x and w, BL(*), is used to define (or update) row I and column J of A.) RETURN END IF END Remarks:  The values of I and J are the row and column indices for the nonzero entries of the matrix A. The value of this entry is AIJ. Set INDCAT=0 if this value defines that entry. Set INDCAT=1 if this entry is to be updated, new entry=old entry+AIJ. A value of I not between 1 and MRELAS, a value of J not between 1 and NVARS, or a value of INDCAT not equal to 0 or 1 are each errors. The contents of IFLAG(K), K=2,...,10, can be used to remember the status (of the process of defining the matrix entries) between calls to 'NAME'( ) by DSPLP( ). On entry to 'NAME'( ), only the values 1 or 2 will be in IFLAG(1).  More than 2*NVARS*MRELAS definitions of the matrix elements is considered an error because it suggests an infinite loop in the user-written subprogram 'NAME'( ).  Any matrix element not provided by 'NAME'( ) is defined to be zero. are .LT. 0. The meaning of these values can be found by reading the diagnostic message in the output file, or by looking for NVARS). Computing with the Matrix A after Calling DSPLP( ) 3 16). If SWITCH=0  option is off; DSPLP( ) chooses the initial basis. =1  option is on; user provides the initial basis. data set =MRELAS indices of basis; order is arbitrary. number of constraint equations. ITERATIONS. written by garbage.  Messages: (I1). IN ABOVE MESSAGE, I1=         0 not applicable or not stated by selected source not a workspace argument

## 3. `NVARS`

input `scalar` argument; Fortran declaration `INTEGER`, Rust ABI type `*mut crate::FortranInteger`, and scalar. columns.  The vectors costs, x, and w respectively have NVARS, NVARS, and MRELAS number of entries. The input for the problem includes the problem dimensions, and       LBM  =8*MRELAS LAMAT determines the length of the sparse matrix storage area. The value of LBM determines the amount of storage available to decompose and update the active basis matrix. and       LBM  =8*MRELAS These values will be as shown unless the user changes them by means of input in the option array PRGOPT(*).  The value of LAMAT determines the length of the sparse matrix "staging" area. For reasons of efficiency the user may want to increase the value of LAMAT.  The value of LBM determines the amount of storage available to decompose and update the active basis matrix. Due to exhausting the working space because of fill-in, it may be necessary for the user to increase the value of LBM. (If this situation occurs an informative diagnostic is printed is used to define (or update) row I and column J of A.) RETURN END IF END Remarks:  The values of I and J are the row and column indices for the nonzero entries of the matrix A. The value of this entry is AIJ. Set INDCAT=0 if this value defines that entry. Set INDCAT=1 if this entry is to be updated, new entry=old entry+AIJ. A value of I not between 1 and MRELAS, a value of J not between 1 and NVARS, or a value of INDCAT not equal to 0 or 1 are each errors. The contents of IFLAG(K), K=2,...,10, can be used to remember the status (of the process of defining the matrix entries) between calls to 'NAME'( ) by DSPLP( ). On entry to 'NAME'( ), only the values 1 or 2 will be in IFLAG(1).  More than 2*NVARS*MRELAS definitions of the matrix elements is considered an error because it suggests an infinite loop in the user-written subprogram 'NAME'( ).  Any matrix element not provided by 'NAME'( ) is defined to be zero. 3 DEFINE THE ARRAY COSTS(*) FOR THE OBJECTIVE FUNCTION. STOP END (NVARS+MRELAS).  A value for an 16). If SWITCH=0  option is off; DSPLP( ) chooses the initial basis. =1  option is on; user provides the initial basis. data set =MRELAS indices of basis; order is arbitrary. number of dependent variables. LATP = 6 ITERATIONS. written by garbage.  Messages: (I1). IN ABOVE MESSAGE, I1=         0 columns.  The vectors costs, x, and w respectively have NVARS, NVARS, and MRELAS number of entries. The input for the problem includes the problem dimensions, and       LBM  =8*MRELAS LAMAT determines the length of the sparse matrix storage area. The value of LBM determines the amount of storage available to decompose and update the active basis matrix. and       LBM  =8*MRELAS These values will be as shown unless the user changes them by means of input in the option array PRGOPT(*).  The value of LAMAT determines the length of the sparse matrix "staging" area. For reasons of efficiency the user may want to increase the value of LAMAT.  The value of LBM determines the amount of storage available to decompose and update the active basis matrix. Due to exhausting the working space because of fill-in, it may be necessary for the user to increase the value of LBM. (If this situation occurs an informative diagnostic is printed is used to define (or update) row I and column J of A.) RETURN END IF END Remarks:  The values of I and J are the row and column indices for the nonzero entries of the matrix A. The value of this entry is AIJ. Set INDCAT=0 if this value defines that entry. Set INDCAT=1 if this entry is to be updated, new entry=old entry+AIJ. A value of I not between 1 and MRELAS, a value of J not between 1 and NVARS, or a value of INDCAT not equal to 0 or 1 are each errors. The contents of IFLAG(K), K=2,...,10, can be used to remember the status (of the process of defining the matrix entries) between calls to 'NAME'( ) by DSPLP( ). On entry to 'NAME'( ), only the values 1 or 2 will be in IFLAG(1).  More than 2*NVARS*MRELAS definitions of the matrix elements is considered an error because it suggests an infinite loop in the user-written subprogram 'NAME'( ).  Any matrix element not provided by 'NAME'( ) is defined to be zero. 3 DEFINE THE ARRAY COSTS(*) FOR THE OBJECTIVE FUNCTION. STOP END (NVARS+MRELAS).  A value for an 16). If SWITCH=0  option is off; DSPLP( ) chooses the initial basis. =1  option is on; user provides the initial basis. data set =MRELAS indices of basis; order is arbitrary. number of dependent variables. LATP = 6 ITERATIONS. written by garbage.  Messages: (I1). IN ABOVE MESSAGE, I1=         0 not applicable or not stated by selected source not a workspace argument

## 4. `COSTS`

input `array` argument; Fortran declaration `DOUBLE PRECISION`, Rust ABI type `*mut f64`, and rank 1; dimensions (*). 1. 1. 1. PLACE THE NONZERO INFORMATION ABOUT THE MATRIX IN DATTRV(*). DEFINE COL. 1: STOP END Normally, DSPLP( ) computes this scale factor to be the reciprocal of the max. norm of the vector costs after the column scaling has been applied. If SWITCH=0  option is off; DSPLP( ) computes COSTSC. =1  option is on; user provides COSTSC. data set =COSTSC LATP=4 not stated by selected source not applicable or not stated by selected source not a workspace argument

## 5. `PRGOPT`

input `array` argument; Fortran declaration `DOUBLE PRECISION`, Rust ABI type `*mut f64`, and rank 1; dimensions (*). must be at least 1. The exact lengths will be determined by user-required options and data transferred to the subprogram DUSRMT( ). The values of LW and LIW, the lengths of the arrays WORK(*) and IWORK(*), must satisfy the inequalities 1.D0. For users with more sophisticated needs, DSPLP( ) provides several options that may be used to take advantage of more detailed knowledge of the problem or satisfy other utilitarian needs. The complete description of how to use this option array to utilize additional subprogram features is found under the heading  of DSPLP( ) Subprogram Options in the Extended Usage Instructions. Briefly, the user should note the following value of the parameter KEY and the corresponding task or feature desired before turning to that document. Value     Brief Statement of Purpose for Option of KEY The matrix A has MRELAS rows and NVARS columns.  The vectors costs, x, and w respectively have NVARS, NVARS, and must be at least 1. The exact lengths will be determined by user-required options and data transferred to the subprogram DUSRMT( ) ( or 'NAME'). The values of LW and LIW, the lengths of the arrays WORK(*) and IWORK(*), must satisfy the inequalities are type REAL. The user interacts with the contents of IFLAG(*) to direct the appropriate action.  The algorithmic steps are as follows. Test IFLAG(1). IF(IFLAG(1).EQ.1) THEN Initialize the necessary pointers and data for defining the matrix A.  The contents of IFLAG(K), K=2,...,10, may be used for storage of the pointers.  This array remains intact between calls to 'NAME'( ) by DSPLP( ). RETURN END IF IF(IFLAG(1).EQ.2) THEN Define one set of values for I,J,AIJ, and INDCAT. Each nonzero entry of A must be defined this way. These values can be defined in any convenient order. (It is most efficient to define the data by columns in the order 1,...,NVARS; within each column define the entries in the order 1,...,MRELAS.) If this is the last matrix value to be defined or updated, then set IFLAG(1)=3. (When I and J are positive and respectively no larger are passed as arguments directly from DSPLP( ) to 'NAME'( ). defined program options.  In this usage mode the array DATTRV(*) may now contain any (type REAL) data that the user needs to define the matrix A.  Both arrays PRGOPT(*) and 1.D0. For users with more sophisticated needs, DSPLP( ) provides several options that may be used to take advantage of more detailed knowledge of the problem or satisfy other utilitarian needs. The complete description of how to use this option array to utilize additional subprogram features is found under the heading "Usage of DSPLP( ) Subprogram Options." Briefly, the user should note the following value of the parameter KEY and the corresponding task or feature desired before turning to that section. Value     Brief Statement of Purpose for Option of KEY 6. 1 DEFINE THE WORKING ARRAY LENGTHS. LINK1 (link to first entry of next group) KEY1 (KEY to the option change) SWITCH1 (on/off switch for the option) data value .       . .       . .       . LINK2 (link to first entry of next group) KEY2 (KEY to option change) SWITCH2 (on/off switch for the option) data value ...     . .       . .       . 1 (no more options to change) A value of LINK that is .LE.0 or .GT. 10000 is an error. 1 (or PRGOPT(1)=1.D0). Options: 4 50 1 LIMIT THE NUMBER OF SIMPLEX STEPS TO 100; KEY=58. 8 58 1 100 SAVE THE PARTIAL RESULTS, AFTER 100 STEPS, ON FORTRAN UNIT 2; KEY=57. 11 57 1 NO MORE OPTIONS TO CHANGE. 1 The user makes the CALL statement for DSPLP( ) at this point. Now to restart, using the partial results after 100 steps, define AGAIN INFORM DSPLP( ) THAT THIS IS A MAXIMIZATION PROBLEM. 4 50 1 RESTART, USING SAVED PARTIAL RESULTS; KEY=55. 7 55 1 NO MORE OPTIONS TO CHANGE.  THE SUBPROGRAM DSPLP( ) IS NO LONGER LIMITED TO 100 SIMPLEX STEPS BUT WILL RUN UNTIL COMPLETION OR 1 The user now makes a CALL to subprogram DSPLP( ) to compute the solution. must be at least 1. The exact lengths will be determined by user-required options and data transferred to the subprogram DUSRMT( ). The values of LW and LIW, the lengths of the arrays WORK(*) and IWORK(*), must satisfy the inequalities 1.D0. For users with more sophisticated needs, DSPLP( ) provides several options that may be used to take advantage of more detailed knowledge of the problem or satisfy other utilitarian needs. The complete description of how to use this option array to utilize additional subprogram features is found under the heading  of DSPLP( ) Subprogram Options in the Extended Usage Instructions. Briefly, the user should note the following value of the parameter KEY and the corresponding task or feature desired before turning to that document. Value     Brief Statement of Purpose for Option of KEY The matrix A has MRELAS rows and NVARS columns.  The vectors costs, x, and w respectively have NVARS, NVARS, and must be at least 1. The exact lengths will be determined by user-required options and data transferred to the subprogram DUSRMT( ) ( or 'NAME'). The values of LW and LIW, the lengths of the arrays WORK(*) and IWORK(*), must satisfy the inequalities are type REAL. The user interacts with the contents of IFLAG(*) to direct the appropriate action.  The algorithmic steps are as follows. Test IFLAG(1). IF(IFLAG(1).EQ.1) THEN Initialize the necessary pointers and data for defining the matrix A.  The contents of IFLAG(K), K=2,...,10, may be used for storage of the pointers.  This array remains intact between calls to 'NAME'( ) by DSPLP( ). RETURN END IF IF(IFLAG(1).EQ.2) THEN Define one set of values for I,J,AIJ, and INDCAT. Each nonzero entry of A must be defined this way. These values can be defined in any convenient order. (It is most efficient to define the data by columns in the order 1,...,NVARS; within each column define the entries in the order 1,...,MRELAS.) If this is the last matrix value to be defined or updated, then set IFLAG(1)=3. (When I and J are positive and respectively no larger are passed as arguments directly from DSPLP( ) to 'NAME'( ). defined program options.  In this usage mode the array DATTRV(*) may now contain any (type REAL) data that the user needs to define the matrix A.  Both arrays PRGOPT(*) and 1.D0. For users with more sophisticated needs, DSPLP( ) provides several options that may be used to take advantage of more detailed knowledge of the problem or satisfy other utilitarian needs. The complete description of how to use this option array to utilize additional subprogram features is found under the heading "Usage of DSPLP( ) Subprogram Options." Briefly, the user should note the following value of the parameter KEY and the corresponding task or feature desired before turning to that section. Value     Brief Statement of Purpose for Option of KEY 6. 1 DEFINE THE WORKING ARRAY LENGTHS. LINK1 (link to first entry of next group) KEY1 (KEY to the option change) SWITCH1 (on/off switch for the option) data value .       . .       . .       . LINK2 (link to first entry of next group) KEY2 (KEY to option change) SWITCH2 (on/off switch for the option) data value ...     . .       . .       . 1 (no more options to change) A value of LINK that is .LE.0 or .GT. 10000 is an error. 1 (or PRGOPT(1)=1.D0). Options: 4 50 1 LIMIT THE NUMBER OF SIMPLEX STEPS TO 100; KEY=58. 8 58 1 100 SAVE THE PARTIAL RESULTS, AFTER 100 STEPS, ON FORTRAN UNIT 2; KEY=57. 11 57 1 NO MORE OPTIONS TO CHANGE. 1 The user makes the CALL statement for DSPLP( ) at this point. Now to restart, using the partial results after 100 steps, define AGAIN INFORM DSPLP( ) THAT THIS IS A MAXIMIZATION PROBLEM. 4 50 1 RESTART, USING SAVED PARTIAL RESULTS; KEY=55. 7 55 1 NO MORE OPTIONS TO CHANGE.  THE SUBPROGRAM DSPLP( ) IS NO LONGER LIMITED TO 100 SIMPLEX STEPS BUT WILL RUN UNTIL COMPLETION OR 1 The user now makes a CALL to subprogram DSPLP( ) to compute the solution. not applicable or not stated by selected source not a workspace argument

## 6. `DATTRV`

input `array` argument; Fortran declaration `DOUBLE PRECISION`, Rust ABI type `*mut f64`, and rank 1; dimensions (*). must be at least 1. The exact lengths will be determined by user-required options and data transferred to the subprogram DUSRMT( ). The values of LW and LIW, the lengths of the arrays WORK(*) and IWORK(*), must satisfy the inequalities Each column (numbered J) requires (floating point) data con- sisting of the value (-J) followed by pairs of values.  Each pair consists of the row index immediately followed by the value of the matrix at that entry.  A value of J=0 signals that there are no more columns.  The required length of is 2*no. of nonzeros + NVARS + 1. must be at least 1. The exact lengths will be determined by user-required options and data transferred to the subprogram DUSRMT( ) ( or 'NAME'). The values of LW and LIW, the lengths of the arrays WORK(*) and IWORK(*), must satisfy the inequalities are type REAL. The user interacts with the contents of IFLAG(*) to direct the appropriate action.  The algorithmic steps are as follows. Test IFLAG(1). IF(IFLAG(1).EQ.1) THEN Initialize the necessary pointers and data for defining the matrix A.  The contents of IFLAG(K), K=2,...,10, may be used for storage of the pointers.  This array remains intact between calls to 'NAME'( ) by DSPLP( ). RETURN END IF IF(IFLAG(1).EQ.2) THEN Define one set of values for I,J,AIJ, and INDCAT. Each nonzero entry of A must be defined this way. These values can be defined in any convenient order. (It is most efficient to define the data by columns in the order 1,...,NVARS; within each column define the entries in the order 1,...,MRELAS.) If this is the last matrix value to be defined or updated, then set IFLAG(1)=3. (When I and J are positive and respectively no larger are passed as arguments directly from DSPLP( ) to 'NAME'( ). remain intact between calls to 'NAME'( ) by DSPLP( ). Here is a subprogram that communicates the matrix values for A, as represented in DATTRV(*), to DSPLP( ).  This subprogram, called DUSRMT( ), is included as part of the DSPLP( ) package. This subprogram 'decodes' the array DATTRV(*) and defines the nonzero entries of the matrix  A for DSPLP( ) to store.  This listing is presented here as a guide and example for the users who find it necessary to write their own subroutine for this purpose.  The contents of DATTRV(*) are given below in the description of that parameter. SUBROUTINE DUSRMT(I,J,AIJ, INDCAT,PRGOPT,DATTRV,IFLAG) DIMENSION PRGOPT(*),DATTRV(*),IFLAG(10) IF(IFLAG(1).EQ.1) THEN THIS IS THE INITIALIZATION STEP.  THE VALUES OF IFLAG(K),K=2,3,4, ARE RESPECTIVELY THE COLUMN INDEX, THE ROW INDEX (OR THE NEXT COL. INDEX), AND THE POINTER TO THE MATRIX ENTRY'S VALUE WITHIN 0.) SIGNIFYING NO DATA. 0.) SIGNIFYING NO DATA. THEN I = 0 J = 0 IFLAG(1) = 3 ELSE IFLAG(3)= DATTRV(2) IFLAG(4)= 3 END IF RETURN ELSE J=IFLAG(2) I=IFLAG(3) L=IFLAG(4) IF(I.EQ.0) THEN SIGNAL THAT ALL OF THE NONZERO ENTRIES HAVE BEEN DEFINED. IFLAG(1)=3 RETURN ELSE IF(I.LT.0) THEN SIGNAL THAT A SWITCH IS MADE TO A NEW COLUMN. J=-I L=L+1 END IF UPDATE THE INDICES AND POINTERS FOR THE NEXT ENTRY. IFLAG(2)=J IFLAG(4)=L+2 INDCAT=0 DENOTES THAT ENTRIES OF THE MATRIX ARE ASSIGNED THE VALUES FROM DATTRV(*).  NO ACCUMULATION IS PERFORMED. INDCAT=0 RETURN END IF END Each column (numbered J) requires (floating point) data con- sisting of the value (-J) followed by pairs of values.  Each pair consists of the row index immediately followed by the value of the matrix at that entry.  A value of J=0 signals that there are no more columns.  (See "Example of DSPLP( ) Usage," below.) The dimension of DATTRV(*) must be 2*no. of nonzeros + NVARS + 1 in this usage.  No checking of the array length is done by the subprogram package. If the Save/Restore feature is in use (see options with KEY=55,57 for details of checkpointing and restarting) 1 1 1. 2 1. DEFINE COL. 2: 2 1 3. 2 2. 3 2. DEFINE COL. 3: 3 1 4. 3 1. 0 CONSTRAIN X1,X2 TO BE NONNEGATIVE. LET X3 HAVE NO BOUNDS. written subprogram or that data has been over-written. Messages: DSPLP( ). MORE THAN 2*NVARS*MRELAS ITERS. DEFINING OR UPDATING MATRIX DATA. must be at least 1. The exact lengths will be determined by user-required options and data transferred to the subprogram DUSRMT( ). The values of LW and LIW, the lengths of the arrays WORK(*) and IWORK(*), must satisfy the inequalities Each column (numbered J) requires (floating point) data con- sisting of the value (-J) followed by pairs of values.  Each pair consists of the row index immediately followed by the value of the matrix at that entry.  A value of J=0 signals that there are no more columns.  The required length of is 2*no. of nonzeros + NVARS + 1. must be at least 1. The exact lengths will be determined by user-required options and data transferred to the subprogram DUSRMT( ) ( or 'NAME'). The values of LW and LIW, the lengths of the arrays WORK(*) and IWORK(*), must satisfy the inequalities are type REAL. The user interacts with the contents of IFLAG(*) to direct the appropriate action.  The algorithmic steps are as follows. Test IFLAG(1). IF(IFLAG(1).EQ.1) THEN Initialize the necessary pointers and data for defining the matrix A.  The contents of IFLAG(K), K=2,...,10, may be used for storage of the pointers.  This array remains intact between calls to 'NAME'( ) by DSPLP( ). RETURN END IF IF(IFLAG(1).EQ.2) THEN Define one set of values for I,J,AIJ, and INDCAT. Each nonzero entry of A must be defined this way. These values can be defined in any convenient order. (It is most efficient to define the data by columns in the order 1,...,NVARS; within each column define the entries in the order 1,...,MRELAS.) If this is the last matrix value to be defined or updated, then set IFLAG(1)=3. (When I and J are positive and respectively no larger are passed as arguments directly from DSPLP( ) to 'NAME'( ). remain intact between calls to 'NAME'( ) by DSPLP( ). Here is a subprogram that communicates the matrix values for A, as represented in DATTRV(*), to DSPLP( ).  This subprogram, called DUSRMT( ), is included as part of the DSPLP( ) package. This subprogram 'decodes' the array DATTRV(*) and defines the nonzero entries of the matrix  A for DSPLP( ) to store.  This listing is presented here as a guide and example for the users who find it necessary to write their own subroutine for this purpose.  The contents of DATTRV(*) are given below in the description of that parameter. SUBROUTINE DUSRMT(I,J,AIJ, INDCAT,PRGOPT,DATTRV,IFLAG) DIMENSION PRGOPT(*),DATTRV(*),IFLAG(10) IF(IFLAG(1).EQ.1) THEN THIS IS THE INITIALIZATION STEP.  THE VALUES OF IFLAG(K),K=2,3,4, ARE RESPECTIVELY THE COLUMN INDEX, THE ROW INDEX (OR THE NEXT COL. INDEX), AND THE POINTER TO THE MATRIX ENTRY'S VALUE WITHIN 0.) SIGNIFYING NO DATA. 0.) SIGNIFYING NO DATA. THEN I = 0 J = 0 IFLAG(1) = 3 ELSE IFLAG(3)= DATTRV(2) IFLAG(4)= 3 END IF RETURN ELSE J=IFLAG(2) I=IFLAG(3) L=IFLAG(4) IF(I.EQ.0) THEN SIGNAL THAT ALL OF THE NONZERO ENTRIES HAVE BEEN DEFINED. IFLAG(1)=3 RETURN ELSE IF(I.LT.0) THEN SIGNAL THAT A SWITCH IS MADE TO A NEW COLUMN. J=-I L=L+1 END IF UPDATE THE INDICES AND POINTERS FOR THE NEXT ENTRY. IFLAG(2)=J IFLAG(4)=L+2 INDCAT=0 DENOTES THAT ENTRIES OF THE MATRIX ARE ASSIGNED THE VALUES FROM DATTRV(*).  NO ACCUMULATION IS PERFORMED. INDCAT=0 RETURN END IF END Each column (numbered J) requires (floating point) data con- sisting of the value (-J) followed by pairs of values.  Each pair consists of the row index immediately followed by the value of the matrix at that entry.  A value of J=0 signals that there are no more columns.  (See "Example of DSPLP( ) Usage," below.) The dimension of DATTRV(*) must be 2*no. of nonzeros + NVARS + 1 in this usage.  No checking of the array length is done by the subprogram package. If the Save/Restore feature is in use (see options with KEY=55,57 for details of checkpointing and restarting) 1 1 1. 2 1. DEFINE COL. 2: 2 1 3. 2 2. 3 2. DEFINE COL. 3: 3 1 4. 3 1. 0 CONSTRAIN X1,X2 TO BE NONNEGATIVE. LET X3 HAVE NO BOUNDS. written subprogram or that data has been over-written. Messages: DSPLP( ). MORE THAN 2*NVARS*MRELAS ITERS. DEFINING OR UPDATING MATRIX DATA. not applicable or not stated by selected source not a workspace argument

## 7. `BL`

input `array` argument; Fortran declaration `DOUBLE PRECISION`, Rust ABI type `*mut f64`, and rank 1; dimensions (*). are not used. For values of I between NVARS+1 and NVARS+MRELAS, NVARS) .LE. BU(I), BU(I) is ok). are not used. A value of IND(*) not equal to 1,2,3 or 4 is an error.  When are not used. For values of I between NVARS+1 and NVARS+MRELAS, NVARS) .LE. BU(I), BU(I) is ok). are not used. A value of IND(*) not equal to 1,2,3 or 4 is an error.  When 0. 0. 5. 4. CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. not stated by selected source not applicable or not stated by selected source not a workspace argument

## 8. `BU`

input `array` argument; Fortran declaration `DOUBLE PRECISION`, Rust ABI type `*mut f64`, and rank 1; dimensions (*). and IND(*).  Only the nonzero entries of the matrix A are passed to DSPLP( ). are not used. For values of I between NVARS+1 and NVARS+MRELAS, are not used. A value of IND(*) not equal to 1,2,3 or 4 is an error.  When indicates infeasibility and is an error. and IND(*). are not used. For values of I between NVARS+1 and NVARS+MRELAS, are not used. A value of IND(*) not equal to 1,2,3 or 4 is an error.  When indicates infeasibility and is an error.  These arrays can be changed by the user between restarts.  See options with KEY=55,57 for details of checkpointing and restarting. 5. 3. CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. not stated by selected source not applicable or not stated by selected source not a workspace argument

## 9. `IND`

status-output `status` argument; Fortran declaration `INTEGER`, Rust ABI type `*mut crate::FortranInteger`, and rank 1; dimensions (*). 1, then X(J) .GE. BL(J); BU(J) is not used. 2, then X(J) .LE. BU(J); BL(J) is not used. 3, then BL(J) .LE. X(J) .LE. BU(J),(BL(J)=BU(J) ok) 4, then X(J) is free to have any value, NVARS) .GE. BL(I); BU(I) is not used. NVARS) .LE. BU(I); BL(I) is not used. NVARS) .LE. BU(I), NVARS) is free to have any value, 3, BL(I) must be .LE. BU(I).  The condition BL(I).GT. 1, then X(J) .GE. BL(J); BU(J) is not used. 2, then X(J) .LE. BU(J); BL(J) is not used. 3, then BL(J) .LE. X(J) .LE. BU(J),(BL(J)=BU(J) ok) 4, then X(J) is free to have any value, NVARS) .GE. BL(I); BU(I) is not used. NVARS) .LE. BU(I); BL(I) is not used. NVARS) .LE. BU(I), NVARS) is free to have any value, 3, BL(I) must be .LE. BU(I).  The condition BL(I).GT. 4 or IND(I+NVARS)=-4, 3.  Further, if the input value of IND(J) =1, then BU(J)=BL(J); =2, then BL(J)=BU(J); =4, then BL(J)=0.,BU(J)=0. (The J-th variable in x has been restricted to an appropriate feasible value.) The negative output value for IND(*) allows the user to identify those constraints that are not satisfied or those variables that would cause unbounded values of the objective function.  Note that the absolute value of IND(*), together with BL(*) and BU(*), are valid input to DSPLP( ).  In the case of infeasibility the sum of magnitudes of the infeasible values is minimized.  Thus one could reenter DSPLP( ) with these components of x or w now fixed at their present values.  This involves setting 3, and BL(*) = BU(*). 1 1 4 CONSTRAIN W1=5,W2.LE.3, AND W3.GE.4. 3 2 1 INDICATE THAT NO MODIFICATIONS TO OPTIONS ARE IN USE. CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. 1, then X(J) .GE. BL(J); BU(J) is not used. 2, then X(J) .LE. BU(J); BL(J) is not used. 3, then BL(J) .LE. X(J) .LE. BU(J),(BL(J)=BU(J) ok) 4, then X(J) is free to have any value, NVARS) .GE. BL(I); BU(I) is not used. NVARS) .LE. BU(I); BL(I) is not used. NVARS) .LE. BU(I), NVARS) is free to have any value, 3, BL(I) must be .LE. BU(I).  The condition BL(I).GT. 1, then X(J) .GE. BL(J); BU(J) is not used. 2, then X(J) .LE. BU(J); BL(J) is not used. 3, then BL(J) .LE. X(J) .LE. BU(J),(BL(J)=BU(J) ok) 4, then X(J) is free to have any value, NVARS) .GE. BL(I); BU(I) is not used. NVARS) .LE. BU(I); BL(I) is not used. NVARS) .LE. BU(I), NVARS) is free to have any value, 3, BL(I) must be .LE. BU(I).  The condition BL(I).GT. 4 or IND(I+NVARS)=-4, 3.  Further, if the input value of IND(J) =1, then BU(J)=BL(J); =2, then BL(J)=BU(J); =4, then BL(J)=0.,BU(J)=0. (The J-th variable in x has been restricted to an appropriate feasible value.) The negative output value for IND(*) allows the user to identify those constraints that are not satisfied or those variables that would cause unbounded values of the objective function.  Note that the absolute value of IND(*), together with BL(*) and BU(*), are valid input to DSPLP( ).  In the case of infeasibility the sum of magnitudes of the infeasible values is minimized.  Thus one could reenter DSPLP( ) with these components of x or w now fixed at their present values.  This involves setting 3, and BL(*) = BU(*). 1 1 4 CONSTRAIN W1=5,W2.LE.3, AND W3.GE.4. 3 2 1 INDICATE THAT NO MODIFICATIONS TO OPTIONS ARE IN USE. CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. not applicable or not stated by selected source not a workspace argument

## 10. `INFO`

status-output `status` argument; Fortran declaration `INTEGER`, Rust ABI type `*mut crate::FortranInteger`, and scalar. 1) includes optimal values for x and w in PRIMAL(*), optimal values for dual variables of the equations A*x=w and the simple bounds on x in  DUALS(*), and the indices of the basic columns, 1 the solution has been computed.  In this case are .LT. 0. The meaning of these values can be found by reading the diagnostic message in the output file, or by looking for 1,-2 or -3.    See the Extended Usage Instructions for further details. 1).  A value of IBASIS(I) between 1 and NVARS corresponds to the variable 1) includes optimal values for x and w in PRIMAL(*), optimal values for dual variables of the equations A*x=w and the simple bounds on x in  DUALS(*), and the indices of the basic columns in 28 is obtained as an output parameter.) 1 the solution has been computed.  In this case are .LT. 0. The meaning of these values can be found by reading the diagnostic message in the output file, or by looking for 1,-2 or -3.  For INFO=-1 or -3 certain comp- onents of the vectors x or w will not satisfy the input bounds. If component J of X or component I of W does not satisfy its input 2 or -3 certain components of the vector x could not be used as basic variables because the objective function would have become unbounded. In particular if component J of x corresponds to such a variable, 1).  A value of IBASIS(I) between 1 and NVARS corresponds to the variable CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. 14. This helps prevent using invalid but positive values of LINK that will probably extend beyond the program limits of PRGOPT(*). Unrecognized values of KEY are ignored.  If the value of SWITCH is zero then the option is turned off.  For any other value of SWITCH the option is turned on.  This is used to allow easy changing of options without rewriting PRGOPT(*).  The order of the options is arbitrary and any number of options can be changed with the following restriction.  To prevent cycling in processing of the option array PRGOPT(*), a count of the number of options changed is maintained.  Whenever this count exceeds 1000 an error message 15) is printed and the subprogram returns. In the following description of the options, the value of LATP indicates the amount of additional storage that a particular option requires.  The sum of all of these values (plus one) is the minimum dimension for the array PRGOPT(*). If a user is satisfied with the nominal form of DSPLP( ), 1), (6) the value of the objective function, (7) the values of the vectors x and w, (8) the dual variables for the constraints A*x=w and the bounded components of x, (9) the indices of the basic variables, (10) the number of revised simplex method iterations, (11) the number of full decompositions of the basis matrix. The second level of output (SWITCH=2) includes all for SWITCH=1 plus (12) the iteration number, (13) the column number to enter the basis, (14) the column number to leave the basis, (15) the length of the step taken. The third level of output (SWITCH=3) includes all for SWITCH=2 plus (16) critical quantities required in the revised simplex method. This output is rather voluminous.  It is intended to be used as a diagnostic tool in case of a failure in DSPLP( ). If SWITCH=0  option is off; no printed output. =1  summary output. =2  lots of output. =3  even more output. data set =empty LATP=3 23 or =-24, respectively).  Normally, DSPLP( ) uses identity matrix columns which correspond to the components of w. This option would normally not be used when restarting from a previously saved run (KEY=57). In numbering the unknowns, the components of x are numbered (1-NVARS) and the components 16). If SWITCH=0  option is off; DSPLP( ) chooses the initial basis. =1  option is on; user provides the initial basis. data set =MRELAS indices of basis; order is arbitrary. 22. Both ASMALL and ABIG must be positive with ASMALL .LE. ABIG.  Otherwise,  an error message is returned, 17. If SWITCH=0  option is off; no checking of the data matrix is done =1  option is on; checking is done. data set =ASMALL ABIG LATP=5 1) includes optimal values for x and w in PRIMAL(*), optimal values for dual variables of the equations A*x=w and the simple bounds on x in  DUALS(*), and the indices of the basic columns, 1 the solution has been computed.  In this case are .LT. 0. The meaning of these values can be found by reading the diagnostic message in the output file, or by looking for 1,-2 or -3.    See the Extended Usage Instructions for further details. 1).  A value of IBASIS(I) between 1 and NVARS corresponds to the variable 1) includes optimal values for x and w in PRIMAL(*), optimal values for dual variables of the equations A*x=w and the simple bounds on x in  DUALS(*), and the indices of the basic columns in 28 is obtained as an output parameter.) 1 the solution has been computed.  In this case are .LT. 0. The meaning of these values can be found by reading the diagnostic message in the output file, or by looking for 1,-2 or -3.  For INFO=-1 or -3 certain comp- onents of the vectors x or w will not satisfy the input bounds. If component J of X or component I of W does not satisfy its input 2 or -3 certain components of the vector x could not be used as basic variables because the objective function would have become unbounded. In particular if component J of x corresponds to such a variable, 1).  A value of IBASIS(I) between 1 and NVARS corresponds to the variable CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. 14. This helps prevent using invalid but positive values of LINK that will probably extend beyond the program limits of PRGOPT(*). Unrecognized values of KEY are ignored.  If the value of SWITCH is zero then the option is turned off.  For any other value of SWITCH the option is turned on.  This is used to allow easy changing of options without rewriting PRGOPT(*).  The order of the options is arbitrary and any number of options can be changed with the following restriction.  To prevent cycling in processing of the option array PRGOPT(*), a count of the number of options changed is maintained.  Whenever this count exceeds 1000 an error message 15) is printed and the subprogram returns. In the following description of the options, the value of LATP indicates the amount of additional storage that a particular option requires.  The sum of all of these values (plus one) is the minimum dimension for the array PRGOPT(*). If a user is satisfied with the nominal form of DSPLP( ), 1), (6) the value of the objective function, (7) the values of the vectors x and w, (8) the dual variables for the constraints A*x=w and the bounded components of x, (9) the indices of the basic variables, (10) the number of revised simplex method iterations, (11) the number of full decompositions of the basis matrix. The second level of output (SWITCH=2) includes all for SWITCH=1 plus (12) the iteration number, (13) the column number to enter the basis, (14) the column number to leave the basis, (15) the length of the step taken. The third level of output (SWITCH=3) includes all for SWITCH=2 plus (16) critical quantities required in the revised simplex method. This output is rather voluminous.  It is intended to be used as a diagnostic tool in case of a failure in DSPLP( ). If SWITCH=0  option is off; no printed output. =1  summary output. =2  lots of output. =3  even more output. data set =empty LATP=3 23 or =-24, respectively).  Normally, DSPLP( ) uses identity matrix columns which correspond to the components of w. This option would normally not be used when restarting from a previously saved run (KEY=57). In numbering the unknowns, the components of x are numbered (1-NVARS) and the components 16). If SWITCH=0  option is off; DSPLP( ) chooses the initial basis. =1  option is on; user provides the initial basis. data set =MRELAS indices of basis; order is arbitrary. 22. Both ASMALL and ABIG must be positive with ASMALL .LE. ABIG.  Otherwise,  an error message is returned, 17. If SWITCH=0  option is off; no checking of the data matrix is done =1  option is on; checking is done. data set =ASMALL ABIG LATP=5 not applicable or not stated by selected source not a workspace argument

## 11. `PRIMAL`

output `array` argument; Fortran declaration `DOUBLE PRECISION`, Rust ABI type `*mut f64`, and rank 1; dimensions (*). and W(I)=PRIMAL(I+NVARS).  The dual variables for the equations A*x=w are in the array DUALS(I)=dual for equation number I.  The dual value for the component X(J) that has an upper or lower bound (or both) is returned in and W(I)=PRIMAL(I+NVARS).  The dual variables for the equations A*x=w are in the array DUALS(I)=dual for equation number I.  The dual value for the component X(J) that has an upper or lower bound (or both) is returned in CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. STOP END and PRGOPT(*). (2) Problem dimensions MRELAS,NVARS. (3) The types of and values for the bounds on x and w, and the values of the components of the vector costs. (4) Whether optimization problem is minimization or maximization. (5) Whether steepest edge or smallest reduced cost criteria used for exchanging variables in the revised simplex method. and W(I)=PRIMAL(I+NVARS).  The dual variables for the equations A*x=w are in the array DUALS(I)=dual for equation number I.  The dual value for the component X(J) that has an upper or lower bound (or both) is returned in and W(I)=PRIMAL(I+NVARS).  The dual variables for the equations A*x=w are in the array DUALS(I)=dual for equation number I.  The dual value for the component X(J) that has an upper or lower bound (or both) is returned in CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. STOP END and PRGOPT(*). (2) Problem dimensions MRELAS,NVARS. (3) The types of and values for the bounds on x and w, and the values of the components of the vector costs. (4) Whether optimization problem is minimization or maximization. (5) Whether steepest edge or smallest reduced cost criteria used for exchanging variables in the revised simplex method. not applicable or not stated by selected source not a workspace argument

## 12. `DUALS`

output `array` argument; Fortran declaration `DOUBLE PRECISION`, Rust ABI type `*mut f64`, and rank 1; dimensions (*). are .LT. 0. The meaning of these values can be found by reading the diagnostic message in the output file, or by looking for are .LT. 0. The meaning of these values can be found by reading the diagnostic message in the output file, or by looking for CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. and PRGOPT(*). (2) Problem dimensions MRELAS,NVARS. (3) The types of and values for the bounds on x and w, and the values of the components of the vector costs. (4) Whether optimization problem is minimization or maximization. (5) Whether steepest edge or smallest reduced cost criteria used for exchanging variables in the revised simplex method. are .LT. 0. The meaning of these values can be found by reading the diagnostic message in the output file, or by looking for are .LT. 0. The meaning of these values can be found by reading the diagnostic message in the output file, or by looking for CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. and PRGOPT(*). (2) Problem dimensions MRELAS,NVARS. (3) The types of and values for the bounds on x and w, and the values of the components of the vector costs. (4) Whether optimization problem is minimization or maximization. (5) Whether steepest edge or smallest reduced cost criteria used for exchanging variables in the revised simplex method. not applicable or not stated by selected source not a workspace argument

## 13. `IBASIS`

input `array` argument; Fortran declaration `INTEGER`, Rust ABI type `*mut crate::FortranInteger`, and rank 1; dimensions (*). 1,...,MRELAS A value of IBASIS(I) between NVARS+1 and NVARS+ NVARS). Long Description: SUBROUTINE DSPLP(DUSRMT,MRELAS,NVARS,COSTS,PRGOPT,DATTRV, 1,...,MRELAS A value of IBASIS(I) between NVARS+1 and NVARS+ NVARS). Computing with the Matrix A after Calling DSPLP( ) CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. and PRGOPT(*). (2) Problem dimensions MRELAS,NVARS. (3) The types of and values for the bounds on x and w, and the values of the components of the vector costs. (4) Whether optimization problem is minimization or maximization. (5) Whether steepest edge or smallest reduced cost criteria used for exchanging variables in the revised simplex method. 1,...,MRELAS A value of IBASIS(I) between NVARS+1 and NVARS+ NVARS). Long Description: SUBROUTINE DSPLP(DUSRMT,MRELAS,NVARS,COSTS,PRGOPT,DATTRV, 1,...,MRELAS A value of IBASIS(I) between NVARS+1 and NVARS+ NVARS). Computing with the Matrix A after Calling DSPLP( ) CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. and PRGOPT(*). (2) Problem dimensions MRELAS,NVARS. (3) The types of and values for the bounds on x and w, and the values of the components of the vector costs. (4) Whether optimization problem is minimization or maximization. (5) Whether steepest edge or smallest reduced cost criteria used for exchanging variables in the revised simplex method. not applicable or not stated by selected source not a workspace argument

## 14. `WORK`

workspace `workspace` argument; Fortran declaration `DOUBLE PRECISION`, Rust ABI type `*mut f64`, and rank 1; dimensions (*). are respectively floating point and type INTEGER working arrays for DSPLP( ) and its subprograms.  The lengths of these arrays are respectively are respectively floating point and type INTEGER working arrays for DSPLP( ) and its subprograms.  The lengths of these arrays are respectively are the same arguments as in the call to DSPLP( ).  The parameter IPLACE is a single INTEGER working variable. The data structure used for storage of the matrix A within DSPLP() corresponds to sequential storage by columns as defined in SAND78-0785.  Note that the names of the subprograms LNNZRS(), LCHNGS(),LINITM(),LLOC(),LRWPGE(), and LRWVIR() have been changed to DPNNZR(),DPCHNG(),PINITM(),IPLOC(),DPRWPG(), and DPRWVR() respectively.  The error processing subprogram LERROR() is no longer used; XERMSG() is used instead. CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. are allocated to the sparse matrix storage and the sparse linear equation solver, respectively. LAMAT must be .GE. NVARS+7 and LBM must be positive. If SWITCH=0  option is off; LAMAT=4*NVARS+7 LBM  =8*MRELAS. =1  option is on. data set =LAMAT LBM LATP=5 IS NOT LONG ENOUGH. LW MUST BE (I1) AND LIW MUST BE (I2). IN ABOVE MESSAGE, I1=         0 IN ABOVE MESSAGE, I2=         0 are respectively floating point and type INTEGER working arrays for DSPLP( ) and its subprograms.  The lengths of these arrays are respectively are respectively floating point and type INTEGER working arrays for DSPLP( ) and its subprograms.  The lengths of these arrays are respectively are the same arguments as in the call to DSPLP( ).  The parameter IPLACE is a single INTEGER working variable. The data structure used for storage of the matrix A within DSPLP() corresponds to sequential storage by columns as defined in SAND78-0785.  Note that the names of the subprograms LNNZRS(), LCHNGS(),LINITM(),LLOC(),LRWPGE(), and LRWVIR() have been changed to DPNNZR(),DPCHNG(),PINITM(),IPLOC(),DPRWPG(), and DPRWVR() respectively.  The error processing subprogram LERROR() is no longer used; XERMSG() is used instead. CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. are allocated to the sparse matrix storage and the sparse linear equation solver, respectively. LAMAT must be .GE. NVARS+7 and LBM must be positive. If SWITCH=0  option is off; LAMAT=4*NVARS+7 LBM  =8*MRELAS. =1  option is on. data set =LAMAT LBM LATP=5 IS NOT LONG ENOUGH. LW MUST BE (I1) AND LIW MUST BE (I2). IN ABOVE MESSAGE, I1=         0 IN ABOVE MESSAGE, I2=         0 not applicable or not stated by selected source

## 15. `LW`

input `scalar` argument; Fortran declaration `INTEGER`, Rust ABI type `*mut crate::FortranInteger`, and scalar. .GE. 4*NVARS+ 8*MRELAS+LAMAT+  LBM is wrong.)  The values of LAMAT and LBM nominally are must satisfy the inequalities noted above under the heading "Fortran Declarations Required:" It is an error if either value is too small. .GE. 4*NVARS+ 8*MRELAS+LAMAT+  LBM is wrong.)  The values of LAMAT and LBM nominally are must satisfy the inequalities noted above under the heading "Fortran Declarations Required." It is an error if either value is too small. 079 CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. .GE. 4*NVARS+ 8*MRELAS+LAMAT+  LBM is wrong.)  The values of LAMAT and LBM nominally are must satisfy the inequalities noted above under the heading "Fortran Declarations Required:" It is an error if either value is too small. .GE. 4*NVARS+ 8*MRELAS+LAMAT+  LBM is wrong.)  The values of LAMAT and LBM nominally are must satisfy the inequalities noted above under the heading "Fortran Declarations Required." It is an error if either value is too small. 079 CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. not applicable or not stated by selected source not a workspace argument

## 16. `IWORK`

workspace `workspace` argument; Fortran declaration `INTEGER`, Rust ABI type `*mut crate::FortranInteger`, and rank 1; dimensions (*). are respectively floating point and type INTEGER working arrays for DSPLP( ) and its subprograms.  The lengths of these arrays are respectively are respectively floating point and type INTEGER working arrays for DSPLP( ) and its subprograms.  The lengths of these arrays are respectively are the same arguments as in the call to DSPLP( ).  The parameter IPLACE is a single INTEGER working variable. The data structure used for storage of the matrix A within DSPLP() corresponds to sequential storage by columns as defined in SAND78-0785.  Note that the names of the subprograms LNNZRS(), LCHNGS(),LINITM(),LLOC(),LRWPGE(), and LRWVIR() have been changed to DPNNZR(),DPCHNG(),PINITM(),IPLOC(),DPRWPG(), and DPRWVR() respectively.  The error processing subprogram LERROR() is no longer used; XERMSG() is used instead. CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. are allocated to the sparse matrix storage and the sparse linear equation solver, respectively. LAMAT must be .GE. NVARS+7 and LBM must be positive. If SWITCH=0  option is off; LAMAT=4*NVARS+7 LBM  =8*MRELAS. =1  option is on. data set =LAMAT LBM LATP=5 IS NOT LONG ENOUGH. LW MUST BE (I1) AND LIW MUST BE (I2). IN ABOVE MESSAGE, I1=         0 IN ABOVE MESSAGE, I2=         0 are respectively floating point and type INTEGER working arrays for DSPLP( ) and its subprograms.  The lengths of these arrays are respectively are respectively floating point and type INTEGER working arrays for DSPLP( ) and its subprograms.  The lengths of these arrays are respectively are the same arguments as in the call to DSPLP( ).  The parameter IPLACE is a single INTEGER working variable. The data structure used for storage of the matrix A within DSPLP() corresponds to sequential storage by columns as defined in SAND78-0785.  Note that the names of the subprograms LNNZRS(), LCHNGS(),LINITM(),LLOC(),LRWPGE(), and LRWVIR() have been changed to DPNNZR(),DPCHNG(),PINITM(),IPLOC(),DPRWPG(), and DPRWVR() respectively.  The error processing subprogram LERROR() is no longer used; XERMSG() is used instead. CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. are allocated to the sparse matrix storage and the sparse linear equation solver, respectively. LAMAT must be .GE. NVARS+7 and LBM must be positive. If SWITCH=0  option is off; LAMAT=4*NVARS+7 LBM  =8*MRELAS. =1  option is on. data set =LAMAT LBM LATP=5 IS NOT LONG ENOUGH. LW MUST BE (I1) AND LIW MUST BE (I2). IN ABOVE MESSAGE, I1=         0 IN ABOVE MESSAGE, I2=         0 not applicable or not stated by selected source

## 17. `LIW`

input `scalar` argument; Fortran declaration `INTEGER`, Rust ABI type `*mut crate::FortranInteger`, and scalar. NVARS+11*MRELAS+LAMAT+2*LBM It is an error if they do not both satisfy these inequalities. (The subprogram will inform the user of the required lengths is wrong.)  The values of LAMAT and LBM nominally are must satisfy the inequalities noted above under the heading "Fortran Declarations Required:" It is an error if either value is too small. NVARS+11*MRELAS+LAMAT+2*LBM It is an error if they do not both satisfy these inequalities. (The subprogram will inform the user of the required lengths is wrong.)  The values of LAMAT and LBM nominally are must satisfy the inequalities noted above under the heading "Fortran Declarations Required." It is an error if either value is too small. 103 CALL DSPLP(DUSRMT,MRELAS,NVARS,COSTS,PRGOPT,DATTRV, CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. NVARS+11*MRELAS+LAMAT+2*LBM It is an error if they do not both satisfy these inequalities. (The subprogram will inform the user of the required lengths is wrong.)  The values of LAMAT and LBM nominally are must satisfy the inequalities noted above under the heading "Fortran Declarations Required:" It is an error if either value is too small. NVARS+11*MRELAS+LAMAT+2*LBM It is an error if they do not both satisfy these inequalities. (The subprogram will inform the user of the required lengths is wrong.)  The values of LAMAT and LBM nominally are must satisfy the inequalities noted above under the heading "Fortran Declarations Required." It is an error if either value is too small. 103 CALL DSPLP(DUSRMT,MRELAS,NVARS,COSTS,PRGOPT,DATTRV, CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. not applicable or not stated by selected source not a workspace argument

# Return value

This is a Fortran subroutine and has no direct return value. Its results, status, and any persistent solver state are communicated through the documented arguments.

# Callback contract

Callback arguments use the reviewed ABI shown by their Rust function-pointer type. They are invoked synchronously by the native call, must remain valid until it returns, must uphold every documented input/output extent, and **must not unwind** through Fortran. A callback must not retain or free caller-owned native buffers unless the source contract expressly permits it.

# Status and error values

under the heading: List of DSPLP( ) Error and Diagnostic Messages. BL(*),BU(*),IND(*) and Diagnostic Messages." The diagnostic messages are printed using the error processing subprogram XERMSG( ) with error category LEVEL=1. See the document "Brief Instr. for Using the Sandia Math. Subroutine Library," SAND79-2382, Nov., 1980, for further inform- ation about resetting the usual response to a diagnostic message. BL(*),BU(*),IND(*) Sparse Matrix Subprograms DPNNZR(),DPCHNG(),DPRWPG(),DPRWVR(), PINITM(),IPLOC() Mass Storage File Subprograms SOPENM(),SCLOSM(),DREADP(),DWRITP() Basic Linear Algebra Subprograms DCOPY(),DASUM(),DDOT() Sparse Matrix Basis Handling Subprograms LA05AD(),LA05BD(), LA05CD(),LA05ED(),MC20AD() Vector Output Subprograms DVOUT(),IVOUT() Machine-sensitive Subprograms I1MACH( ),D1MACH( ), SOPENM(),SCLOSM(),DREADP(),DWRITP(). COMMON Block Used of equations.  Normally, PHI = 1.D0, but in some environments it may be necessary to reset PHI to the range 0.001-0.01.  This is particularly important for machines with short word lengths. if SWITCH = 0 option is off; PHI=1.D0. = 1 option is on. Data Set  = PHI LATP=4 2. One of the variables in either the vector x or w was con- strained at a bound.  Otherwise the objective function value, (transpose of costs)*x, would not have a finite optimum. Message: DSPLP( ). THE PROBLEM APPEARS TO HAVE NO FINITE SOLN. 3.  Both of the conditions of 1. and 2. above have occurred. Message: DSPLP( ). THE PROBLEM APPEARS TO BE INFEASIBLE AND TO HAVE NO FINITE SOLN. DSPLP( ). ROW INDEX (I1) OR COLUMN INDEX (I2) IS OUT OF RANGE. IN ABOVE MESSAGE, I1=         1 IN ABOVE MESSAGE, I2=        12 DSPLP( ). INDICATION FLAG (I1) FOR MATRIX DATA MUST BE EITHER 0 OR 1. IN ABOVE MESSAGE, I1=        12 DSPLP( ).  LOWER BOUND (R1) AND UPPER BOUND (R2) FOR INDEP. VARIABLE (I1) ARE NOT CONSISTENT. IN ABOVE MESSAGE, I1=         1 IN ABOVE MESSAGE, R1=    0. IN ABOVE MESSAGE, R2=    -.1000000000E+01 DSPLP( ).  LOWER BOUND (R1) AND UPPER BOUND (R2) FOR DEP. VARIABLE (I1) ARE NOT CONSISTENT. IN ABOVE MESSAGE, I1=         1 IN ABOVE MESSAGE, R1=    0. IN ABOVE MESSAGE, R2=    -.1000000000E+01 DSPLP( ). OPTION ARRAY PROCESSING IS CYCLING. DSPLP( ). AN INDEX OF USER-SUPPLIED BASIS IS OUT OF RANGE. DSPLP( ). SIZE PARAMETERS FOR MATRIX MUST BE SMALLEST AND LARGEST MAGNITUDES OF NONZERO ENTRIES. DSPLP( ). THE NUMBER OF REVISED SIMPLEX STEPS BETWEEN CHECK-POINTS MUST BE POSITIVE. DSPLP( ). FILE NUMBERS FOR SAVED DATA AND MATRIX PAGES MUST BE POSITIVE AND NOT EQUAL. DSPLP( ). USER-DEFINED VALUE OF LAMAT (I1) MUST BE .GE. NVARS+7. IN ABOVE MESSAGE, I1=         1 DSPLP( ). USER-DEFINED VALUE OF LBM MUST BE .GE. 0.

# Workspace and array requirements

- `DUSRMT`: not a workspace argument
- `MRELAS`: not a workspace argument
- `NVARS`: not a workspace argument
- `COSTS`: not a workspace argument
- `PRGOPT`: not a workspace argument
- `DATTRV`: not a workspace argument
- `BL`: not a workspace argument
- `BU`: not a workspace argument
- `IND`: not a workspace argument
- `INFO`: not a workspace argument
- `PRIMAL`: not a workspace argument
- `DUALS`: not a workspace argument
- `IBASIS`: not a workspace argument
- `WORK`: are respectively floating point and type INTEGER working arrays for DSPLP( ) and its subprograms.  The lengths of these arrays are respectively are respectively floating point and type INTEGER working arrays for DSPLP( ) and its subprograms.  The lengths of these arrays are respectively are the same arguments as in the call to DSPLP( ).  The parameter IPLACE is a single INTEGER working variable. The data structure used for storage of the matrix A within DSPLP() corresponds to sequential storage by columns as defined in SAND78-0785.  Note that the names of the subprograms LNNZRS(), LCHNGS(),LINITM(),LLOC(),LRWPGE(), and LRWVIR() have been changed to DPNNZR(),DPCHNG(),PINITM(),IPLOC(),DPRWPG(), and DPRWVR() respectively.  The error processing subprogram LERROR() is no longer used; XERMSG() is used instead. CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. are allocated to the sparse matrix storage and the sparse linear equation solver, respectively. LAMAT must be .GE. NVARS+7 and LBM must be positive. If SWITCH=0  option is off; LAMAT=4*NVARS+7 LBM  =8*MRELAS. =1  option is on. data set =LAMAT LBM LATP=5 IS NOT LONG ENOUGH. LW MUST BE (I1) AND LIW MUST BE (I2). IN ABOVE MESSAGE, I1=         0 IN ABOVE MESSAGE, I2=         0
- `LW`: not a workspace argument
- `IWORK`: are respectively floating point and type INTEGER working arrays for DSPLP( ) and its subprograms.  The lengths of these arrays are respectively are respectively floating point and type INTEGER working arrays for DSPLP( ) and its subprograms.  The lengths of these arrays are respectively are the same arguments as in the call to DSPLP( ).  The parameter IPLACE is a single INTEGER working variable. The data structure used for storage of the matrix A within DSPLP() corresponds to sequential storage by columns as defined in SAND78-0785.  Note that the names of the subprograms LNNZRS(), LCHNGS(),LINITM(),LLOC(),LRWPGE(), and LRWVIR() have been changed to DPNNZR(),DPCHNG(),PINITM(),IPLOC(),DPRWPG(), and DPRWVR() respectively.  The error processing subprogram LERROR() is no longer used; XERMSG() is used instead. CALCULATE VAL, THE MINIMAL VALUE OF THE OBJECTIVE FUNCTION. are allocated to the sparse matrix storage and the sparse linear equation solver, respectively. LAMAT must be .GE. NVARS+7 and LBM must be positive. If SWITCH=0  option is off; LAMAT=4*NVARS+7 LBM  =8*MRELAS. =1  option is on. data set =LAMAT LBM LATP=5 IS NOT LONG ENOUGH. LW MUST BE (I1) AND LIW MUST BE (I2). IN ABOVE MESSAGE, I1=         0 IN ABOVE MESSAGE, I2=         0
- `LIW`: not a workspace argument

# ABI notes

- Canonical Rust path: `slatec_sys::linear_programming::dsplp`
- Original SLATEC routine: `DSPLP`
- Native symbol: `dsplp_`
- ABI fingerprint: `unavailable`
- Exact Netlib source file: [DSPLP](https://www.netlib.org/slatec/src/dsplp.f)

# Safety

Every raw pointer must be non-null unless this argument contract expressly permits null, correctly aligned, and valid for its documented readable or writable extent. Preserve Fortran column-major layout, length, leading-dimension, workspace, callback-lifetime, aliasing, and provider-runtime requirements. The native routine does not retain ordinary argument pointers beyond this call.
