# Purpose

Usage: DOUBLE PRECISION L2, L3, M2, M3, L1MIN, L1MAX, THRCOF(NDIM) INTEGER NDIM, IER CALL DRC3JJ (L2, L3, M2, M3, L1MIN, L1MAX, THRCOF, NDIM, IER) Although conventionally the parameters of the vector addition coefficients satisfy certain restrictions, such as being integers or integers plus 1/2, the restrictions imposed on input to this subroutine are somewhat weaker. See, for example, Section 27.9 of Abramowitz and Stegun or Appendix C of Volume II of A. Messiah. The restrictions imposed by this subroutine are

# Description

This canonical unsafe binding exposes original SLATEC routine `DRC3JJ`. Its documented behavior is taken from the selected, source-hash-verified provider prologue; the exact implementation source is [DRC3JJ](https://www.netlib.org/slatec/src/drc3jj.f).

# Arguments

## `L2`

**Direction:** `input`. **Fortran type:** `DOUBLE PRECISION`. **Rust ABI type:** `*mut f64`. **Shape:** scalar.

Parameter in 3j symbol. GE. ABS(M2) and L3. ABS(M3); 2. L2+ABS(M2) and L3+ABS(M3) must be integers;.

## `L3`

**Direction:** `input`. **Fortran type:** `DOUBLE PRECISION`. **Rust ABI type:** `*mut f64`. **Shape:** scalar.

Parameter in 3j symbol.

## `M2`

**Direction:** `input`. **Fortran type:** `DOUBLE PRECISION`. **Rust ABI type:** `*mut f64`. **Shape:** scalar.

Parameter in 3j symbol.

## `M3`

**Direction:** `input`. **Fortran type:** `DOUBLE PRECISION`. **Rust ABI type:** `*mut f64`. **Shape:** scalar.

Parameter in 3j symbol.

## `L1MIN`

**Direction:** `input-output`. **Fortran type:** `DOUBLE PRECISION`. **Rust ABI type:** `*mut f64`. **Shape:** scalar.

Smallest allowable L1 in 3j symbol. must be a non-negative integer, where.

## `L1MAX`

**Direction:** `input-output`. **Fortran type:** `DOUBLE PRECISION`. **Rust ABI type:** `*mut f64`. **Shape:** scalar.

Largest allowable L1 in 3j symbol. must be a non-negative integer, where L2+L3 and L1MIN=MAX(ABS(L2-L3),ABS(M2+M3)). If the conventional restrictions are satisfied, then these restrictions are met. The user should be cautious in using input parameters that do not satisfy the conventional restrictions. For example, the the subroutine produces values of f(L1) = ( L1 2. 5 5.

## `THRCOF`

**Direction:** `output`. **Fortran type:** `DOUBLE PRECISION`. **Rust ABI type:** `*mut f64`. **Shape:** rank 1; dimensions (NDIM).

Set of 3j coefficients generated by evaluating the 3j symbol for all allowed values of L1. THRCOF(I) will contain f(L1MIN+I-1), I=1,2,. ,L1MAX+L1MIN+1.

## `NDIM`

**Direction:** `input`. **Fortran type:** `INTEGER`. **Rust ABI type:** `*mut crate::FortranInteger`. **Shape:** scalar.

Declared length of THRCOF in calling program.

## `IER`

**Direction:** `status-output`. **Fortran type:** `INTEGER`. **Rust ABI type:** `*mut crate::FortranInteger`. **Shape:** scalar.

Error flag. IER=0 No errors. IER=1 Either L2. LT. ABS(M2) or L3. ABS(M3).

# Return value

This is a Fortran subroutine and has no direct return value. Its results, status, and any persistent solver state are communicated through the documented arguments.

# Status and error values

| Status | Value | Meaning |
| --- | ---: | --- |
| `IER` | `0` | No errors. |
| `IER` | `1` | Either L2.LT.ABS(M2) or L3.LT.ABS(M3). |
| `IER` | `2` | Either L2+ABS(M2) or L3+ABS(M3) non-integer. |
| `IER` | `3` | L1MAX-L1MIN not an integer. |
| `IER` | `4` | L1MAX less than L1MIN. |
| `IER` | `5` | NDIM less than L1MAX-L1MIN+1. |

# Workspace and array requirements

- `THRCOF`: not a workspace argument

# ABI notes

- Canonical Rust path: `slatec_sys::special::drc3jj`
- Original SLATEC routine: `DRC3JJ`
- Native symbol: `drc3jj_`
- ABI fingerprint: `subroutine:void(mut_f64,mut_f64,mut_f64,mut_f64,mut_f64,mut_f64,mut_f64_ptr_rank1,mut_i32,mut_i32)`
- Exact Netlib source file: [DRC3JJ](https://www.netlib.org/slatec/src/drc3jj.f)

# Safety

Every raw pointer must be non-null unless this argument contract expressly permits null, correctly aligned, and valid for its documented readable or writable extent. Preserve Fortran column-major layout, length, leading-dimension, workspace, callback-lifetime, aliasing, and provider-runtime requirements. The native routine does not retain ordinary argument pointers beyond this call.
