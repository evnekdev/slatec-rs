[package]
name = "slatec-consumer-prebuilt-blocked"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
slatec = { path = "../../../crates/slatec", default-features = false, features = ["std", "prebuilt", "special-gamma"] }
